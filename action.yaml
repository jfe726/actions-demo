name: jfe-example-action
description: An example Action
author: J

branding:
  icon: bell
  color: green

runs:
  using: composite
  steps:
    - name: Get Date & Time
      shell: bash
      run: |
        date -Iseconds